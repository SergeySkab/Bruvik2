# Secrets Folder

Store local sensitive files here.

This directory is intentionally excluded from:
- Git commits (`.gitignore`)
- Docker build context (`.dockerignore`)
- Vercel deployment context (`.vercelignore`)
- Netlify deployment context (`.netlifyignore`)

Never place production secrets in repository files.

